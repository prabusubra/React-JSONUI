version: '1.0'
 steps:
  Build
   type: build
   describtion: Building React-JSONUI
   image-name: react-jsonui
   working_directory: ./
   tag: '${{CF_BRANCH_TAG_NORMALIZED}}-${{CF_SHORT_REVISION}}'
   dockerfile: Dockerfile
   no_cache: true
   metadata: 
    set:
      - build_style: non-multi-stage